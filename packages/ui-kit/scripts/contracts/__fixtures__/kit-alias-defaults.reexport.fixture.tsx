// Fixture for extract.test.ts: a kit component re-exported under another name
// from the file that writes it, which reads that component's body.
export { Item as ReItem } from './kit-alias-defaults.fixture';

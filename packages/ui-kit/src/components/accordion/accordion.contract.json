{
  "$id": "gts.frontx.uikit._.component.v1~frontx.uikit._.accordion.v1",
  "gts_type": "gts.frontx.uikit._.component.v1~",
  "metamodel": "2.0.0",
  "component": "accordion",
  "forwards_to": "gts.frontx.uikit.element.dom_div.v1~",
  "intent": "Show one or more sections at a time, collapsing the rest to their headers.",
  "typical_uses": [
    "A list of independent sections (FAQ, settings groups) too long to show all at once, where the user opens what is relevant",
    "Progressive disclosure of grouped content under named headers"
  ],
  "dont_use_when": [
    {
      "situation": "A single show/hide toggle for one piece of content",
      "instead": {
        "target": "Collapsible",
        "component": "gts.frontx.uikit._.component.v1~frontx.uikit._.collapsible.v1"
      }
    },
    {
      "situation": "Primary navigation between sections that stays visible",
      "instead": {
        "target": "Tabs",
        "component": "gts.frontx.uikit._.component.v1~frontx.uikit._.tabs.v1"
      }
    }
  ],
  "accepts": {
    "content": "specified",
    "components": [
      "gts.frontx.uikit._.component.v1~frontx.uikit._.accordion_item.v1"
    ]
  },
  "invariants": [
    {
      "id": "multiple-false-at-most-one-open",
      "text": "With `multiple` at its default of false, opening one item closes any other open item - at most one item is open at a time."
    },
    {
      "id": "item-values-unique-per-root",
      "text": "Every `AccordionItem`'s `value` must be unique within THIS root - not across roots. Base UI's Accordion.Root keeps its own value state and its own keyboard-navigation list, each scoped to that root's React context; a nested `Accordion` inside another's panel resolves its OWN root context, so its items may safely reuse a value already used by an ancestor root without any collision."
    }
  ],
  "anti_patterns": [
    {
      "dont": "Put an always-visible required form control inside a collapsed panel",
      "do_instead": "Keep required fields outside the accordion, or default that item's panel open - a screen reader user tabbing through the page will not discover a hidden required field until they expand that section"
    },
    {
      "dont": "Reuse the same `value` for two items inside the SAME accordion root",
      "do_instead": "Give every item a unique `value` within its own root - a nested `Accordion` needs no coordination with its ancestor's values, only with its own siblings"
    }
  ],
  "deprecations": {},
  "attestations": {
    "a11y": {
      "outcome": "verified",
      "by": "Base UI's Accordion.Root - keyboard navigation between triggers, and the ARIA expanded/controls wiring"
    },
    "rtl": {
      "outcome": "unknown"
    }
  },
  "prop_statements": {
    "value": {
      "states": "value is an array of the root's own generic Value parameter",
      "because": "`AccordionValue<Value>` is an ARRAY of the type parameter, not a single value - which is exactly the misreading this entry exists to prevent: an agent shown an unconstrained `value` concluded it took a plain string. The schema states the array; what is IN it is the caller's own `Value`, which no schema can state, because the caller chooses it. accordion.md's example uses string item values (`value=\"item-1\"` on the item), so a root value is an array of those."
    },
    "defaultValue": {
      "states": "defaultValue is an array of the root's own generic Value parameter",
      "because": "Same type as `value` above, and unchecked in the same half - the uncontrolled starting point is a LIST of open item values, not one of them."
    },
    "onValueChange": {
      "states": "onValueChange is a callback, not a value",
      "because": "`(value: AccordionValue<Value>, eventDetails) => void` - a function of the generic above plus Base UI's own event-details object. No JSON Schema type covers a function."
    },
    "render": {
      "states": "render replaces the root element with one the caller supplies",
      "because": "Base UI's render prop takes an element or a function of the root's state; the compiler emits one JSON type per property. Replacing the root element means taking over the wrapper the kit's own layout styles (`.root`) are attached to."
    },
    "style": {
      "states": "style accepts a function of the root's state as well as a plain style object",
      "because": "Base UI widens React's `CSSProperties` to `CSSProperties | ((state) => CSSProperties)`; the compiler emits one JSON type per property."
    }
  },
  "examples": {
    "good": [
      {
        "title": "Two sections, single-expansion (the default)",
        "code": "<Accordion>\n  <AccordionItem value=\"item-1\">\n    <AccordionTrigger>Is it accessible?</AccordionTrigger>\n    <AccordionContent>Yes. It adheres to the WAI-ARIA design pattern.</AccordionContent>\n  </AccordionItem>\n  <AccordionItem value=\"item-2\">\n    <AccordionTrigger>Is it styled?</AccordionTrigger>\n    <AccordionContent>Yes, with the kit's own tokens.</AccordionContent>\n  </AccordionItem>\n</Accordion>\n"
      }
    ],
    "bad": [
      {
        "title": "A single section used as a show/hide toggle",
        "code": "<Accordion>\n  <AccordionItem value=\"only\">\n    <AccordionTrigger>Show details</AccordionTrigger>\n    <AccordionContent>Details go here.</AccordionContent>\n  </AccordionItem>\n</Accordion>\n",
        "why": "One section is not what an accordion is for - it carries Base UI's multi-item expansion machinery for nothing to coordinate. Reach for `Collapsible` instead."
      }
    ]
  },
  "family_membership": {
    "name": "accordion",
    "role": "root",
    "members": [
      "gts.frontx.uikit._.component.v1~frontx.uikit._.accordion_content.v1",
      "gts.frontx.uikit._.component.v1~frontx.uikit._.accordion_item.v1",
      "gts.frontx.uikit._.component.v1~frontx.uikit._.accordion_trigger.v1"
    ]
  },
  "withheld": [
    {
      "prop": "orientation",
      "reason": "The root's own stylesheet fixes the layout to a single column (accordion.module.css's `.root { flex-direction: column }`), so the only value `orientation` could add - \"horizontal\" - would put Base UI's horizontal keyboard axis on a vertical layout. Withheld rather than listed: a contract listing it would be this kit's own statement that it works."
    }
  ],
  "x-uikit": {
    "partially_typed_props": {},
    "variant_sources": [],
    "cannot_extract": []
  },
  "props": {
    "title": "UiKit Accordion",
    "type": "object",
    "properties": {
      "className": {
        "type": "string"
      },
      "defaultValue": {
        "type": "array",
        "description": "TS: AccordionValue<Value> | undefined. Not fully expressible in JSON Schema; what the type states beyond the kind above is checked by tsc. defaultValue is an array of the root's own generic Value parameter. Same type as `value` above, and unchecked in the same half - the uncontrolled starting point is a LIST of open item values, not one of them."
      },
      "disabled": {
        "type": "boolean"
      },
      "hiddenUntilFound": {
        "type": "boolean"
      },
      "keepMounted": {
        "type": "boolean"
      },
      "loopFocus": {
        "type": "boolean"
      },
      "multiple": {
        "type": "boolean"
      },
      "onValueChange": {
        "description": "TS: ((value: AccordionValue<Value>, eventDetails: AccordionRootChangeEventDetails) => void) | undefined. The compiler emits one JSON type per property; this type is left to tsc. onValueChange is a callback, not a value. `(value: AccordionValue<Value>, eventDetails) => void` - a function of the generic above plus Base UI's own event-details object. No JSON Schema type covers a function."
      },
      "render": {
        "description": "TS: ReactElement<unknown, string | JSXElementConstructor<any>> | ComponentRenderFn<HTMLProps, AccordionPrimitive.Root.State<Value>> | undefined. The compiler emits one JSON type per property; this type is left to tsc. render replaces the root element with one the caller supplies. Base UI's render prop takes an element or a function of the root's state; the compiler emits one JSON type per property. Replacing the root element means taking over the wrapper the kit's own layout styles (`.root`) are attached to."
      },
      "style": {
        "description": "TS: CSSProperties | ((state: AccordionPrimitive.Root.State<Value>) => CSSProperties | undefined) | undefined. The compiler emits one JSON type per property; this type is left to tsc. style accepts a function of the root's state as well as a plain style object. Base UI widens React's `CSSProperties` to `CSSProperties | ((state) => CSSProperties)`; the compiler emits one JSON type per property."
      },
      "value": {
        "type": "array",
        "description": "TS: AccordionValue<Value> | undefined. Not fully expressible in JSON Schema; what the type states beyond the kind above is checked by tsc. value is an array of the root's own generic Value parameter. `AccordionValue<Value>` is an ARRAY of the type parameter, not a single value - which is exactly the misreading this entry exists to prevent: an agent shown an unconstrained `value` concluded it took a plain string. The schema states the array; what is IN it is the caller's own `Value`, which no schema can state, because the caller chooses it. accordion.md's example uses string item values (`value=\"item-1\"` on the item), so a root value is an array of those."
      }
    },
    "required": [],
    "unevaluatedProperties": {
      "x-uikit-classification": "unchecked"
    }
  }
}

{
  "$id": "gts.frontx.uikit._.component.v1~frontx.uikit._.accordion_trigger.v1",
  "gts_type": "gts.frontx.uikit._.component.v1~",
  "metamodel": "2.0.0",
  "component": "accordion-trigger",
  "forwards_to": "gts.frontx.uikit.element.dom_button.v1~",
  "intent": "The clickable header that expands or collapses its AccordionItem.",
  "typical_uses": [
    "The single header/summary child of an AccordionItem"
  ],
  "dont_use_when": [
    {
      "situation": "A standalone button with no AccordionItem to expand or collapse",
      "instead": {
        "target": "Button",
        "component": "gts.frontx.uikit._.component.v1~frontx.uikit._.button.v1"
      }
    }
  ],
  "accepts": {
    "content": "specified",
    "text": true
  },
  "mounted_in": [
    {
      "container": "AccordionItem",
      "component": "gts.frontx.uikit._.component.v1~frontx.uikit._.accordion_item.v1"
    }
  ],
  "invariants": [
    {
      "id": "always-renders-a-chevron",
      "text": "AccordionTrigger always renders a trailing chevron that rotates on open/close - there is no prop to hide it, matching upstream Base UI."
    }
  ],
  "anti_patterns": [
    {
      "dont": "Pass an icon element through children expecting it before the chevron",
      "do_instead": "Children are the trigger's text label only; the chevron is fixed and always trailing"
    }
  ],
  "deprecations": {},
  "attestations": {
    "a11y": {
      "outcome": "verified",
      "by": "Base UI's Accordion.Header and Accordion.Trigger - focus handling and the aria-expanded/aria-controls wiring"
    },
    "rtl": {
      "outcome": "unknown"
    }
  },
  "prop_statements": {
    "render": {
      "states": "render replaces the trigger element with one the caller supplies",
      "because": "Base UI's render prop takes an element or a function of the trigger's state; the compiler emits one JSON type per property. The trailing chevron is rendered as a child, so it survives a replaced element."
    },
    "style": {
      "states": "style accepts a function of the trigger's state as well as a plain style object",
      "because": "Base UI widens React's `CSSProperties` to `CSSProperties | ((state) => CSSProperties)`; the compiler emits one JSON type per property."
    }
  },
  "unexposed_parts": [
    {
      "part": "Base UI's Accordion.Header and Accordion.Trigger",
      "reason": "The kit's AccordionTrigger composes Base UI's Accordion.Header (a plain wrapper, not separately exposed by the kit) around Accordion.Trigger into one exported component; this contract describes the exported surface, not the two-part internal composition, so Header's own props are not part of it."
    }
  ],
  "examples": {
    "good": [
      {
        "title": "A trigger as an AccordionItem's header",
        "code": "<AccordionItem value=\"item-1\">\n  <AccordionTrigger>Is it accessible?</AccordionTrigger>\n</AccordionItem>\n"
      }
    ],
    "bad": [
      {
        "title": "A trigger with no AccordionItem around it",
        "code": "<AccordionTrigger>Is it accessible?</AccordionTrigger>",
        "why": "AccordionTrigger reads and writes the nearest AccordionItem's open state through Base UI context; outside one, clicking it does nothing."
      }
    ]
  },
  "family_membership": {
    "name": "accordion",
    "role": "part"
  },
  "x-uikit": {
    "partially_typed_props": {},
    "variant_sources": [],
    "cannot_extract": []
  },
  "props": {
    "title": "UiKit AccordionTrigger",
    "type": "object",
    "properties": {
      "className": {
        "type": "string"
      },
      "nativeButton": {
        "type": "boolean"
      },
      "render": {
        "description": "TS: ReactElement<unknown, string | JSXElementConstructor<any>> | ComponentRenderFn<HTMLProps, AccordionTriggerState> | undefined. The compiler emits one JSON type per property; this type is left to tsc. render replaces the trigger element with one the caller supplies. Base UI's render prop takes an element or a function of the trigger's state; the compiler emits one JSON type per property. The trailing chevron is rendered as a child, so it survives a replaced element."
      },
      "style": {
        "description": "TS: CSSProperties | ((state: AccordionTriggerState) => CSSProperties | undefined) | undefined. The compiler emits one JSON type per property; this type is left to tsc. style accepts a function of the trigger's state as well as a plain style object. Base UI widens React's `CSSProperties` to `CSSProperties | ((state) => CSSProperties)`; the compiler emits one JSON type per property."
      }
    },
    "required": [],
    "unevaluatedProperties": {
      "x-uikit-classification": "unchecked"
    }
  }
}

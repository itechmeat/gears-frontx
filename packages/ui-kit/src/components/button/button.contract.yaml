# Semantic overlay for Button - the hand-written half of the contract.
#
# Machine-extractable facts (axis names, axis values, defaults, prop list)
# do NOT belong here: the compiler takes those from button.tsx, and rejects
# an overlay that tries to restate them - one fact, one owner. This file
# carries only what the code cannot express: intent, boundaries, invariants.
component: button

# One sentence, the selection-card headline.
intent: Trigger a single action in the current context.

typical_uses:
  - A one-off action with an immediate effect (submit, confirm, open a dialog)
  - An icon-only action that has an accessible name (pass aria-label)

# Each entry names the alternative - "don't" without "instead" is banned.
# `target` is what a reader acts on, always written. `component` is added
# when the kit ships one: it is a GTS component type id, checked against what
# the kit actually ships, and its ABSENCE is the honest statement that the kit
# ships nothing for this case. GTS tokens are snake_case, kit directories
# kebab-case (navigation_menu -> src/components/navigation-menu).
dont_use_when:
  - situation: Navigation between routes or pages
    instead:
      target: the consuming application's own link component (a plain anchor when it has none)
      note: >-
        This kit ships no Link component, and no kit component is a substitute
        for one: navigation belongs to the app's own router. An action that
        must read as a button while it navigates stays on Button, rendered
        over a real anchor - `render={<a href="..." />}` with
        `nativeButton={false}`.
  - situation: Toggling a persistent on/off state
    instead:
      target: Switch
      component: gts.frontx.uikit._.component.v1~frontx.uikit._.switch.v1

# What may appear inside. `text` says a non-component React node may:
# a string, a number, a fragment, a formatted inline element.
accepts:
  content: specified
  text: true
  # Icons go through the `icon` prop, never through children: the slot is
  # what gets sized, spaced, hidden during `loading` and marked decorative.
  icons_via: icon

invariants:
  - id: icon-only-needs-label
    text: >-
      With `icon` set and no children the button renders icon-only and carries
      no accessible name of its own - `aria-label` is required.
  - id: loading-is-not-native-disabled
    text: >-
      While `loading`, the button stays in the tab order and reports
      `aria-disabled`/`aria-busy`; it never becomes natively `disabled`, so
      `:disabled` selectors and the `disabled` DOM property do not see it.

anti_patterns:
  - dont: Render a spinner next to the label by hand
    do_instead: Use `loading` - it preserves the button's width and accessible name
  - dont: Pass an icon element through children
    do_instead: Use the `icon` prop

# None yet. Shape when one appears:
#   props:
#     tone: { since: "0.5.0", replacement: "variant", hint: "tone=X maps to variant=X" }
deprecations: {}

# One outcome per claim. "failed" (looked at, does not hold) and "unknown"
# (nobody looked) are different answers - say which. `by` names what
# established a verified outcome, so a later reader can re-check it.
attestations:
  a11y:
    outcome: verified
    by: Base UI's own Button primitive plus this contract's loading invariant, exercised by button.test.tsx
  rtl:
    outcome: unknown

# What each property that reaches this contract asserting nothing (or only
# part of its shape) actually states, and why - keyed on the property
# itself. The compiler emits both halves into that property's own
# description, beside the `TS:` text; the conformance suite pairs every key
# here against the properties the schema leaves unasserted, both ways.
prop_statements:
  icon:
    states: icon is a React node, not a value
    because: >-
      `ReactNode` is a union, and the compiler emits one JSON type per property - an element, a string,
      a fragment and `false` are all valid and only tsc can tell them from
      a mistake. The slot exists so the icon gets sized, spaced, hidden
      while `loading` and marked decorative; what may go in it is
      "anything React renders", which is the whole assertion.
  render:
    states: render replaces the rendered element with one the caller supplies
    because: >-
      Base UI's render prop takes an element or a function of the button's
      own state, so its type is a union of a React element and a callback - the compiler emits one JSON type per property. It is what makes
      `render={<a href="..." />}` with `nativeButton={false}` the way to
      keep an action reading as a button while it navigates.
  style:
    states: style accepts a function of the button's state as well as a plain style object
    because: >-
      Base UI widens React's `CSSProperties` to
      `CSSProperties | ((state) => CSSProperties)` so a caller can style off
      the primitive's own state. The compiler emits one JSON type per property, so nothing is asserted; the kit's own styling goes
      through `variant`/`size` and CSS Modules, not through this.

# Canonical snippets. `good` is what to copy, `bad` is what generation
# actually produces when it guesses; every `bad` states why, because a
# counter-example without its reason teaches the shape, not the rule.
examples:
  good:
    - title: Variant and size on a plain action
      code: <Button variant="destructive" size="sm" onClick={handleDelete}>Delete</Button>
    - title: Icon-only, named for assistive tech
      code: <Button variant="ghost" icon={<TrashIcon />} aria-label="Delete item" />
  bad:
    - title: Icon passed through children
      code: <Button variant="ghost"><TrashIcon /> Delete</Button>
      why: >-
        Children are text only. An icon in children misses the slot that sizes
        it, spaces it, hides it while `loading` and marks it `aria-hidden`, so
        it is announced as part of the accessible name.
    - title: Spinner rendered by hand
      code: <Button disabled={saving}>{saving && <Spinner />}Save</Button>
      why: >-
        `loading` exists for this: it keeps the button's width, keeps the
        accessible name and reports `aria-busy` while staying focusable. A
        native `disabled` blurs the button the instant it lands, so the busy
        state is announced to nobody.

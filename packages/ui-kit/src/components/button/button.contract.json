{
  "$id": "gts.frontx.uikit._.component.v1~frontx.uikit._.button.v1",
  "gts_type": "gts.frontx.uikit._.component.v1~",
  "metamodel": "2.0.0",
  "component": "button",
  "forwards_to": "gts.frontx.uikit.element.dom_button.v1~",
  "intent": "Trigger a single action in the current context.",
  "typical_uses": [
    "A one-off action with an immediate effect (submit, confirm, open a dialog)",
    "An icon-only action that has an accessible name (pass aria-label)"
  ],
  "dont_use_when": [
    {
      "situation": "Navigation between routes or pages",
      "instead": {
        "target": "the consuming application's own link component (a plain anchor when it has none)",
        "note": "This kit ships no Link component, and no kit component is a substitute for one: navigation belongs to the app's own router. An action that must read as a button while it navigates stays on Button, rendered over a real anchor - `render={<a href=\"...\" />}` with `nativeButton={false}`."
      }
    },
    {
      "situation": "Toggling a persistent on/off state",
      "instead": {
        "target": "Switch",
        "component": "gts.frontx.uikit._.component.v1~frontx.uikit._.switch.v1"
      }
    }
  ],
  "accepts": {
    "content": "specified",
    "text": true,
    "icons_via": "icon"
  },
  "mounted_in": [
    {
      "container": "AlertAction",
      "component": "gts.frontx.uikit._.component.v1~frontx.uikit._.alert_action.v1"
    }
  ],
  "invariants": [
    {
      "id": "icon-only-needs-label",
      "text": "With `icon` set and no children the button renders icon-only and carries no accessible name of its own - `aria-label` is required."
    },
    {
      "id": "loading-is-not-native-disabled",
      "text": "While `loading`, the button stays in the tab order and reports `aria-disabled`/`aria-busy`; it never becomes natively `disabled`, so `:disabled` selectors and the `disabled` DOM property do not see it."
    }
  ],
  "anti_patterns": [
    {
      "dont": "Render a spinner next to the label by hand",
      "do_instead": "Use `loading` - it preserves the button's width and accessible name"
    },
    {
      "dont": "Pass an icon element through children",
      "do_instead": "Use the `icon` prop"
    }
  ],
  "deprecations": {},
  "attestations": {
    "a11y": {
      "outcome": "verified",
      "by": "Base UI's own Button primitive plus this contract's loading invariant, exercised by button.test.tsx"
    },
    "rtl": {
      "outcome": "unknown"
    }
  },
  "prop_statements": {
    "icon": {
      "states": "icon is a React node, not a value",
      "because": "`ReactNode` has no JSON Schema representation - an element, a string, a fragment and `false` are all valid and only tsc can tell them from a mistake. The slot exists so the icon gets sized, spaced, hidden while `loading` and marked decorative; what may go in it is \"anything React renders\", which is the whole assertion."
    },
    "render": {
      "states": "render replaces the rendered element with one the caller supplies",
      "because": "Base UI's render prop takes an element or a function of the button's own state, so its type is a union of a React element and a callback - neither expressible in JSON Schema. It is what makes `render={<a href=\"...\" />}` with `nativeButton={false}` the way to keep an action reading as a button while it navigates."
    },
    "style": {
      "states": "style accepts a function of the button's state as well as a plain style object",
      "because": "Base UI widens React's `CSSProperties` to `CSSProperties | ((state) => CSSProperties)` so a caller can style off the primitive's own state. A JSON Schema `object` would reject the function form, so nothing is asserted; the kit's own styling goes through `variant`/`size` and CSS Modules, not through this."
    }
  },
  "examples": {
    "good": [
      {
        "title": "Variant and size on a plain action",
        "code": "<Button variant=\"destructive\" size=\"sm\" onClick={handleDelete}>Delete</Button>"
      },
      {
        "title": "Icon-only, named for assistive tech",
        "code": "<Button variant=\"ghost\" icon={<TrashIcon />} aria-label=\"Delete item\" />"
      }
    ],
    "bad": [
      {
        "title": "Icon passed through children",
        "code": "<Button variant=\"ghost\"><TrashIcon /> Delete</Button>",
        "why": "Children are text only. An icon in children misses the slot that sizes it, spaces it, hides it while `loading` and marks it `aria-hidden`, so it is announced as part of the accessible name."
      },
      {
        "title": "Spinner rendered by hand",
        "code": "<Button disabled={saving}>{saving && <Spinner />}Save</Button>",
        "why": "`loading` exists for this: it keeps the button's width, keeps the accessible name and reports `aria-busy` while staying focusable. A native `disabled` blurs the button the instant it lands, so the busy state is announced to nobody."
      }
    ]
  },
  "x-uikit": {
    "partially_typed_props": {
      "icon": {
        "typeText": "ReactNode",
        "optional": true
      }
    },
    "variant_sources": [
      "VariantProps<typeof buttonVariants>"
    ],
    "cannot_extract": []
  },
  "props": {
    "title": "UiKit Button",
    "type": "object",
    "properties": {
      "variant": {
        "type": "string",
        "enum": [
          "default",
          "destructive",
          "outline",
          "secondary",
          "ghost",
          "link",
          "navigation",
          "avatar",
          "utility"
        ],
        "default": "default"
      },
      "size": {
        "type": "string",
        "enum": [
          "default",
          "sm",
          "lg"
        ],
        "default": "default"
      },
      "className": {
        "type": "string"
      },
      "focusableWhenDisabled": {
        "type": "boolean"
      },
      "icon": {
        "description": "Partially typed: ReactNode. No JSON Schema type exists for it; shape checked by tsc, see x-uikit.partially_typed_props. icon is a React node, not a value. `ReactNode` has no JSON Schema representation - an element, a string, a fragment and `false` are all valid and only tsc can tell them from a mistake. The slot exists so the icon gets sized, spaced, hidden while `loading` and marked decorative; what may go in it is \"anything React renders\", which is the whole assertion."
      },
      "loading": {
        "type": "boolean"
      },
      "nativeButton": {
        "type": "boolean"
      },
      "render": {
        "description": "TS: ReactElement<unknown, string | JSXElementConstructor<any>> | ComponentRenderFn<HTMLProps, ButtonState> | undefined. Not expressible in JSON Schema, checked by tsc. render replaces the rendered element with one the caller supplies. Base UI's render prop takes an element or a function of the button's own state, so its type is a union of a React element and a callback - neither expressible in JSON Schema. It is what makes `render={<a href=\"...\" />}` with `nativeButton={false}` the way to keep an action reading as a button while it navigates."
      },
      "style": {
        "description": "TS: CSSProperties | ((state: ButtonState) => CSSProperties | undefined) | undefined. Not expressible in JSON Schema, checked by tsc. style accepts a function of the button's state as well as a plain style object. Base UI widens React's `CSSProperties` to `CSSProperties | ((state) => CSSProperties)` so a caller can style off the primitive's own state. A JSON Schema `object` would reject the function form, so nothing is asserted; the kit's own styling goes through `variant`/`size` and CSS Modules, not through this."
      }
    },
    "required": [],
    "unevaluatedProperties": {
      "x-uikit-classification": "unchecked"
    }
  }
}
